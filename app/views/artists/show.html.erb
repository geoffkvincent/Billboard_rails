<p id="notice" class=<%= notice ? 'success' : ''%>>
<%= notice %>
</p>
<div class="container">
  <div class="image">
  <img 
  src="<%= @artist.image_src %>" alt="<%@artist.name%>" />
  </div>
  <div class="content">
    
   <h1> <%= @artist.name%></h1>
    <div class="description">
   <p> <%= @artist.description%></p>
    </div>
    </div>
    <div class="extra content">
      <%= link_to 'Edit', edit_artist_path(@artist) %> |
      <%= link_to 'Back', artists_path %>
    </div>
  </div>
</div>


